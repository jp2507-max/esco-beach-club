name: Content Compliance (Cannabis Policy)

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cannabis-policy:
    name: Cannabis policy scan
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup Node + Bun + install deps
        uses: ./.github/actions/setup-node-bun-install

      - name: 🔍 Run cannabis policy scan
        run: bun run compliance:cannabis

      - name: 📄 Upload compliance report (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cannabis-policy-report
          path: |
            build/reports/compliance/cannabis-policy.json
            build/reports/compliance/cannabis-policy.md
